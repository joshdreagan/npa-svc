apiVersion: "v1"
kind: "ConfigMap"
metadata:
  name: npa-svc-configmap
data:
  application.properties: |-
    spring.datasource.url=jdbc:mysql://mysql:3306/example?autoReconnect=true&useSSL=false&allowPublicKeyRetrieval=true